package gila.challenge.notificationTest.configs;

import gila.challenge.notificationTest.model.Message;
import gila.challenge.notificationTest.repository.MessageRepository;
import gila.challenge.notificationTest.service.*;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.data.jpa.repository.JpaRepository;

@Configuration
public class AppConfig {
/*
    @Bean
    public MessageRepository messageRepository() {
        // Aquí la implementación de MessageRepository
        return MessageRepository();
    }

    @Bean
    public UserService userService() {
        return new UserService();
    }

    @Bean
    public ChannelService channelService() {
        return new ChannelService();
    }

    @Bean
    public CategoryService categoryService() {
        return new CategoryService();
    }

    @Bean
    public MessageService messageService() {
        return new MessageService(messageRepository(), userService(), channelService(), categoryService());
    }

    @Bean
    public SmsService smsService() {
        return new SmsService(messageService());
    }


 */
/*
    @Bean
    public MessageService messageService() {
        return new MessageService();
    }

    @Bean
    public SmsService smsService() {
        return new SmsService(messageService());
    }



 */
/*
    @Bean
    public UserService userService() {
        return new UserService();
    }

    @Bean
    public ChannelService channelService() {
        return new ChannelService();
    }

    @Bean
    public CategoryService categoryService() {
        return new CategoryService();
    }
*/
}

